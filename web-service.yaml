apiVersion: v1
kind: Service
metadata:
  name: web1-service
spec:
  selector:
    app: nginx
    role: web
    instance: web1
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
